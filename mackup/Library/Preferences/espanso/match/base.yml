# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: sig;
    replace: |-
      Kind Regards,
      Jackson Hoffart
  - trigger: em;
    replace: jackson.hoffart@gmail.com
  - trigger: emw;
    replace: jhoffart@rmi.org
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: date;
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%d/%m/%Y"
  - trigger: td;
    replace: "{{today}}"
    vars:
      - name: today
        type: date
        params:
          format: "%d/%m/%Y"
  - trigger: dmy;
    replace: "{{today}}"
    vars:
      - name: today
        type: date
        params:
          format: "%d/%m/%Y"
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell
