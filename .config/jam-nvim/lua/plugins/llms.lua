return {
  "David-Kunz/gen.nvim",
  commands = { "Gen" },
  keys = {
    -- TODO: figure out how to do this for visual mode
    -- { "v", "<leader>]", ":Gen<CR>" },
    { "<leader>]", ":Gen<CR>" }
  }
}
